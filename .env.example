# Contraseña del panel de administración (profesor)
ADMIN_PASS=tu_contraseña_segura

# Clave secreta para sesiones Flask (genera una aleatoria, ej: python -c "import os; print(os.urandom(24).hex())")
SECRET_KEY=tu_clave_secreta_aqui
